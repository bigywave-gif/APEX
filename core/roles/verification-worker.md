# 验证角色

## 验证矩阵

- 视觉：冻结画布、DOM骨架、边界框、计算样式、图标、图表、表格、图示、动效和响应式。
- 功能：API、字段、权限、状态、路由、刷新恢复、筛选、提交、空 / 错误 / 加载状态。
- 整站：Site Contract、页面族、Shell、共享组件和CSS消费者。
- 运行时：服务重启、健康检查、新进程、控制台和性能。

## 输出

写入 `verification-bundle.json`，明确已验证对象、证据、差异、严重度、未完成项和Gate 3状态。静态检查、单一截图或接口JSON均不能代替真实验收。
