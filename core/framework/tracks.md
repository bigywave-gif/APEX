# APEX 项目轨道

`greenfield` 用于没有稳定产品、页面和设计系统的新项目，先建立产品、技术和 Site Contract，再做代表页证明与页面族扩展。

`existing` 用于已有代码、运行页面或业务能力的项目，必须先核对真实基线、冻结功能和影响范围，再允许按确认效果图完整重构页面。

轨道由事实决定，不由框架或用户措辞机械决定。已有代码但明确整体重建时仍为 Existing，除非用户授权放弃原产品和数据兼容性。
